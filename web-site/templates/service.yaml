apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-sites.name" . }}
  labels:
    {{- include "web-sites.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-sites.selectorLabels" . | nindent 4 }}
